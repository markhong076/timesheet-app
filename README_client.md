# Timesheet Client (React + Vite + TS)

## Run
```bash
cd client
npm i
npm run dev
```
Open http://localhost:5173

Set a custom API base by creating `.env` with:
```
VITE_API_BASE=http://localhost:5000
```
